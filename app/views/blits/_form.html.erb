<%= form_for(@blit) do |f| %>
  <% if @blit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blit.errors.count, "error") %> prohibited this blit from being saved:</h2>

      <ul>
      <% @blit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message, rows: 6%>
 </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
